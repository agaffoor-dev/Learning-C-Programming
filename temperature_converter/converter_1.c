#include <stdio.h>

int lower, upper, fahr, cel, adding; 

lower = 0;

upper = 100;

fahr = 0;

adding = 20;

while (fahr <= upper) {
  (fahr*5)/9=cel;
  printf("%d\t%d", fahr, cel);
  fahr = fahr + adding;
}
